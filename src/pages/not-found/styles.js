const styles = theme => ({
  messageBox: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    height: '100%',
  },
  message: {
    fontSize: '2.6rem',
  }
});
export default styles;